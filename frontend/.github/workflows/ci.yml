name: CI
on:
  push:
    branches: [ main, 'frontend/cart-orders' ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Use Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
      - name: Install dependencies
        run: |
          cd frontend
          npm ci
      - name: Run unit tests
        run: |
          cd frontend
          npm run test -- --coverage
      - name: Build
        run: |
          cd frontend
          npm run build

  # Optional E2E job - requires a running backend and frontend and appropriate secrets.
  # Uncomment and configure if you want E2E in CI.
  # e2e:
  #   needs: build
  #   runs-on: ubuntu-latest
  #   steps:
  #     - uses: actions/checkout@v4
  #     - name: Use Node.js
  #       uses: actions/setup-node@v4
  #       with:
  #         node-version: '18'
  #     - name: Install frontend deps
  #       run: |
  #         cd frontend
  #         npm ci
  #         npx playwright install --with-deps
  #     - name: Start backend (requires configuration)
  #       run: echo "Start your backend here or use a service container"
  #     - name: Start frontend
  #       run: |
  #         cd frontend
  #         nohup npm run dev &
  #     - name: Run Playwright tests
  #       run: |
  #         cd frontend
  #         npx playwright test
